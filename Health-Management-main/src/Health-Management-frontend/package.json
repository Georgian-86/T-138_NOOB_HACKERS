{
  "name": "health-management-frontend",
  "version": "1.0.0",
  "description": "Health Management System Frontend",
  "main": "index.js",
  "scripts": {
    "start": "npx serve dist -s -l 3000",
    "serve": "npx serve dist -s -l 3000",
    "build": "echo 'Build files are already in dist folder'",
    "dev": "npx serve dist -s -l 3000"
  },
  "keywords": [
    "health",
    "management",
    "react",
    "frontend"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
